export default class DashboardController {
    constructor(props = {}) {
        this.config = {
            ...props?.config
        }
    }

    static Settings(){
        return {
            //any return
        }
    }
}
